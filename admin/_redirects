# The Label Board — Admin Control Centre
# Processed before netlify.toml and takes precedence.
# Static files (css/, js/, icon.svg, manifest.json, sw.js) are served
# directly by Netlify before these rules run, so the catch-all below
# only affects paths that do not map to a real file.

# Single-page app: every unmatched route renders the console shell.
/*    /index.html   200
