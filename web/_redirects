# The Label Board — public website
# Netlify serves /features from features.html on its own, so these only cover
# the shorter names people actually type or that we print on things.
/product       /features.html   301
/features      /features.html   301
/price         /pricing.html    301
/plans         /pricing.html    301
/book          /book.html       301
/trial         /book.html       301
/partner       /partners.html   301
/refer         /referrals.html  301
/app           https://app.thelabelboard.com   302
/portal        https://partners.thelabelboard.com  302

/solutions    /index.html#trades   301
/solutions.html /index.html#trades 301

/demo         /book.html      301
/demo.html    /book.html      301

# Short enough to say out loud and to sit in an Instagram bio, which is where
# this one is going. /trial already points at the booking form and stays there.
/join         /waitlist.html  301
/waitlist     /waitlist.html  301
/list         /waitlist.html  301

# --- REVIEWS, MANAGED BY sync_reviews.js, DO NOT EDIT BY HAND ---
# There are no reviews yet, so this page is not somewhere to send anybody.
# The ! is load bearing. Netlify applies a redirect only when no file
# matches the path, and reviews.html is a real file, so without it Netlify
# serves the empty page and ignores the rule. It did exactly that once.
/reviews      /index.html     302!
/reviews.html /index.html     302!
# --- END REVIEWS ---

/support      /contact.html   301
/support.html /contact.html   301

# Anything else falls through to Netlify's own 404 handling, which serves
# 404.html. Do NOT add a catch-all 200 rule here: this is a real multi page
# site, not a single page app, and a catch-all would hide broken links.
